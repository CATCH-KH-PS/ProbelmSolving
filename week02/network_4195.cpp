#include <iostream>
#include <stdio.h>
#include <vector>
#include <string.h>

using namespace std;
int n, m;
int main(void)
{
  scanf("%d", &n);
  for (int i = 0; i < n; i++)
  {
    scanf("%d", &m);
    for (int j = 0; j < m; j++)
    {
    }
  }
  return 0;
}